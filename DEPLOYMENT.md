# راهنمای جامع استقرار سامانه انبارداری واحد اعلام حریق ذوب‌آهن اصفهان روی Railway
**معماری یکپارچه Full-Stack (Frontend + Backend + PostgreSQL)**  
**طراح، تحلیل‌گر و توسعه‌دهنده سیستم:** دکتر احسان ابوالقاسمی

---

## مقدمه و معماری سامانه
سامانه انبارداری واحد اعلام حریق شرکت سهامی ذوب‌آهن اصفهان به صورت یک سامانه مستقل، متمرکز و چندکاربره طراحی شده است. در این معماری، هر دو بخش کلاینت (React / PWA) و سرور (Node.js / Express API) به همراه پایگاه‌داده رابطه ای (PostgreSQL) به طور یکپارچه روی بستر **Railway** اجرا می‌شوند.

کاربران در کلیه سیستم‌ها (رایانه‌های انبار، لپ‌تاپ‌های اداری، تبلت‌ها و گوشی‌های تلفن همراه) تنها یک نشانی اینترنتی واحد (مثلاً `https://firealarmstore-production.up.railway.app`) را باز می‌کنند و به یک منبع داده واحد و بلادرنگ دسترسی خواهند داشت.

```
Browser / Android PWA / Windows / iOS
                ↓
    Railway Unified Application
    ├── Frontend (React SPA / PWA)
    ├── Backend (Express REST API)
    └── Central PostgreSQL Database
```

---

## فهرست گام‌های استقرار
1. ساخت پروژه جدید در Railway
2. اضافه کردن پایگاه داده متمرکز PostgreSQL Plugin
3. اتصال مخزن گیت‌هاب (GitHub Repository)
4. تنظیم متغیرهای محیطی (Environment Variables)
5. تنظیم دستور ساخت (Build Command)
6. تنظیم دستور راه‌اندازی (Start Command)
7. تنظیم Root Directory
8. پیکربندی بررسی سلامت سامانه (Healthcheck)
9. ایجاد و اتصال دامنه عمومی (Public Domain)
10. ورود اولیه به سامانه (First Login)
11. آزمایش همگام‌سازی همزمان چند دستگاه (Multi-Device Sync Test)
12. انتقال داده‌های قبلی مرورگر به پایگاه‌داده سرور (Data Migration)
13. نصب و اتصال برنامه پیش‌رونده وب (PWA) در تلفن همراه
14. مشاهده و پایش لاگ‌های زنده سرور در Railway
15. عیب‌یابی خطاهای متداول (Troubleshooting)

---

### ۱. ساخت پروژه جدید در Railway
1. به وب‌سایت [Railway.app](https://railway.app) رفته و با حساب کاربری گیت‌هاب (GitHub) خود وارد شوید.
2. در داشبورد اصلی، دکمه **«+ New Project»** را انتخاب نمایید.
3. در پنجره باز شده، ابتدا گزینه **«Provision PostgreSQL»** را انتخاب کنید.

---

### ۲. اضافه کردن دیتابیس PostgreSQL Plugin
1. پس از کلیک روی Provision PostgreSQL، چند ثانیه صبر کنید تا کانتینر اختصاصی دیتابیس در همان پروژه راه‌اندازی شود.
2. روی کارت سرویس دیتابیس کلیک کرده و به تب **Variables** بروید.
3. متغیر `DATABASE_URL` به طور خودکار تولید شده و برای اتصال داخلی بین سرویس‌ها در دسترس است.

---

### ۳. اتصال مخزن گیت‌هاب (GitHub Repository)
1. در همان پروژه Railway، دکمه **«+ Create»** یا **«New Service»** را بزنید.
2. گزینه **«GitHub Repo»** را انتخاب کنید.
3. مخزن پروژه خود را انتخاب نمایید:
   `eabolghasemi26/firealarmstore`
4. شاخه اصلی (Branch) را روی `main` قرار دهید. سرویس برنامه اضافه خواهد شد.

---

### ۴. تنظیم متغیرهای محیطی (Environment Variables)
روی سرویس وب (Application Service) کلیک کرده و وارد زبانه **Variables** شوید. متغیرهای زیر را ثبت فرمایید:

| نام متغیر | مقدار پیشنهادی | توضیحات |
|:---|:---|:---|
| `DATABASE_URL` | `${{Postgres.DATABASE_URL}}` | ارجاع خودکار به دیتابیس PostgreSQL ایجاد شده در گام ۲ |
| `DATABASE_SSL` | `false` یا `true` | بر اساس پروتکل اتصال داخلی یا خارجی |
| `JWT_SECRET` | یک عبارت تصادفی و امن (مثلاً ۳۲ کاراکتر) | برای امضا و اعتبارسنجی امن توکن‌های نشست کاربران |
| `JWT_EXPIRES_IN` | `7d` | مدت اعتبار نشست کاربری |
| `ADMIN_USERNAME` | `admin` | نام کاربری مدیر سیستم |
| `ADMIN_INITIAL_PASSWORD` | یک رمز عبور قوی | رمز عبور اولیه مدیر در اولین راه‌اندازی |
| `ADMIN_FULLNAME` | `دکتر احسان ابوالقاسمی` | نام و عنوان رسمی مدیر در سیستم |
| `NODE_ENV` | `production` | فعال‌سازی حالت عملیاتی |
| `PORT` | `3000` | پورت برنامه (به صورت خودکار توسط Railway تزریق می‌شود) |

> **نکته امنیتی:** متغیر `ADMIN_INITIAL_PASSWORD` تنها در نخستین بار که جدول کاربران خالی است جهت ایجاد حساب اولیه استفاده می‌شود و در دیتابیس به صورت هش‌شده (Bcrypt) ذخیره می‌گردد. پس از ورود می‌توانید از طریق بخش تنظیمات رمز را تغییر دهید.

---

### ۵. تنظیم دستور ساخت (Build Command)
در تب **Settings** سرویس، بخش **Build**:
- فیلد **Build Command** را به مقدار زیر تنظیم نمایید (در صورت وجود فایل `railway.json` به صورت خودکار شناسایی می‌شود):
  ```bash
  npm run build
  ```
این دستور کدهای کلاینت React را در پوشه `dist` کامپایل کرده و سرور Express را به صورت باندل بهینه‌سازی شده در `dist/server.cjs` ایجاد می‌کند.

---

### ۶. تنظیم دستور راه‌اندازی (Start Command)
در تب **Settings** سرویس، بخش **Deploy**:
- فیلد **Start Command** را به مقدار زیر تنظیم نمایید:
  ```bash
  npm start
  ```
  (یا `node dist/server.cjs`)

---

### ۷. تنظیم Root Directory
- فیلد **Root Directory** در تنظیمات سرویس باید خالی یا `/` باشد، زیرا تمام ساختار پروژه از ریشه مخزن مدیریت می‌شود.

---

### ۸. پیکربندی بررسی سلامت سامانه (Healthcheck)
در بخش **Deploy** سرویس در تب Settings:
- گزینه **Healthcheck Path** را به مسیر زیر تنظیم نمایید:
  ```
  /api/health
  ```
- فیلد **Healthcheck Timeout** را روی `100` ثانیه قرار دهید.
این آدرس در صورت آماده بودن سرور و اتصال موفق به دیتابیس پاسخ کد ۲۰۰ با محتوای JSON بازمی‌گرداند.

---

### ۹. ایجاد و اتصال دامنه عمومی (Public Domain)
1. در بخش **Settings** سرویس، به قسمت **Networking** بروید.
2. بر روی گزینه **Generate Domain** کلیک کنید.
3. یک نشانی عمومی امن با گواهینامه SSL اختصاص داده می‌شود، مثلاً:
   `https://firealarmstore-production.up.railway.app`
4. (اختیاری) در صورت داشتن دامنه اختصاصی سازمانی (مانند `wms.zobahan.ir`)، می‌توانید از بخش Custom Domain آن را به سرویس متصل فرمایید.

---

### ۱۰. ورود اولیه به سامانه (First Login)
1. آدرس سامانه دریافتی در گام ۹ را در مرورگر باز کنید.
2. با مشخصات زیر وارد شوید:
   - **نام کاربری:** `admin` (یا مقداری که در `ADMIN_USERNAME` تعیین کرده‌اید)
   - **کلمه عبور:** مقدار تنظیم‌شده در `ADMIN_INITIAL_PASSWORD` (پیش‌فرض سیستم: `Ehsan1984*#`)
3. پس از ورود، از منوی بالا به بخش **تنظیمات** رفته و برای افزایش امنیت، کلمه عبور را تغییر دهید.

---

### ۱۱. آزمایش همگام‌سازی همزمان چند دستگاه (Multi-Device Sync Test)
برای اطمینان از عملکرد پایگاه‌داده متمرکز و چندکاربره:
1. در رایانه الف وارد برنامه شوید و در بخش «تعریف کالاها»، یک کالای تستی جدید ثبت کنید.
2. بدون نیاز به خروج یا رفرش، همان آدرس را در گوشی موبایل یا رایانه ب باز کنید.
3. در لیست کالاها مشاهده خواهید کرد که کالای ثبت‌شده فوراً در تمام دستگاه‌ها نمایش داده می‌شود.
4. عملیات ورود کالا، خروج کالا و ثبت تراکنش نیز بلادرنگ روی موجودی قفسه‌ها اعمال می‌شود.

---

### ۱۲. انتقال داده‌های قبلی مرورگر به پایگاه‌داده سرور (Data Migration)
اگر قبلاً اطلاعاتی (کالاها، انبارها، قفسه‌ها یا تراکنش‌ها) در مرورگر خود روی نسخه قدیمی یا GitHub Pages ثبت کرده‌اید:
1. در مرورگری که داده‌های قبلی روی آن است، وارد آدرس جدید سامانه شوید.
2. به بخش **«لاگ‌ها و پشتیبان‌گیری»** > تب **«پشتیبان‌گیری و انتقال»** مراجعه فرمایید.
3. بر روی دکمه آبی‌رنگ **«شروع انتقال داده‌های محلی به سرور مرکزی»** کلیک کنید.
4. سامانه به طور خودکار:
   - یک نسخه پشتیبان JSON برای امنیت دانلود می‌کند.
   - کلیه انبارها، قفسه‌ها، کالاها، موجودی‌ها و تراکنش‌ها را به پایگاه مرکزی PostgreSQL ارسال و ثبت می‌کند.
   - گزارش تفکیکی انتقال را نمایش می‌دهد.

---

### ۱۳. نصب و اتصال برنامه پیش‌رونده وب (PWA) در تلفن همراه
این سامانه یک PWA کامل است و به صورت اپلیکیشن مستقل در گوشی‌های اندروید، آیفون و ویندوز قابل نصب است:
1. آدرس برنامه را در مرورگر Chrome یا Safari تلفن همراه باز کنید.
2. پیام **«افزودن به صفحه اصلی» (Add to Home Screen / Install App)** ظاهر می‌شود. آن را تایید نمایید.
3. آیکون اختصاصی انبارداری واحد اعلام حریق با رنگ سازمانی به منوی برنامه‌های موبایل اضافه خواهد شد.
4. به دلیل پیکربندی `NetworkOnly` برای مسیرهای `/api/`، تمامی استعلام‌های موجودی در اپلیکیشن موبایل به صورت مستقیم و بدون کش کاذب از سرور دریافت می‌شوند.

---

### ۱۴. مشاهده و پایش لاگ‌های زنده سرور در Railway
1. در داشبورد Railway، روی کارت سرویس وب کلیک کنید.
2. تب **Deployments** را باز کرده و آخرین انتشار را انتخاب کنید.
3. در بخش **Deploy Logs**، مراحل ساخت فرانت‌اند و کامپایل سرور را مشاهده می‌کنید.
4. در تب **Runtime Logs**، وضعیت اجرای لحظه‌ای سرور، لاگ استعلام‌ها، وضعیت اتصال PostgreSQL و درخواست‌های کاربران را مشاهده خواهید کرد.

---

### ۱۵. عیب‌یابی خطاهای متداول (Troubleshooting)

#### الف) خطای `Application Error` یا عدم بالا آمدن سرور
- **علت:** عدم تنظیم متغیر `DATABASE_URL` یا اشتباه بودن اتصال به پایگاه‌داده.
- **راه‌حل:** در تب Variables بررسی کنید که مقدار `DATABASE_URL` به درستی با مقدار ارجاع دیتابیس `${{Postgres.DATABASE_URL}}` پر شده باشد.

#### ب) خطای `401 Unauthorized` یا ناتوانی در Login
- **علت:** عدم انطباق رمز عبور یا عدم بارگذاری توکن JWT.
- **راه‌حل:** مطمئن شوید متغیر `ADMIN_INITIAL_PASSWORD` و `JWT_SECRET` در تنظیمات سرویس مقداردهی شده باشند. در صورت لزوم می‌توانید از سربرگ Runtime Logs نحوه ساخت حساب ادمین را مشاهده کنید.

#### ج) خطای ۴۰۴ در هنگام رفرش صفحات (مانند `/inventory`)
- **علت:** عدم هدایت درخواست‌های غیر API به `index.html`.
- **بررسی:** سرور پروژه طوری پیکربندی شده است که تمامی مسیرهای وب به جز `/api/*` را به فایل کامپایل‌شده `dist/index.html` هدایت می‌کند و مشکل رفرش در SPA به طور کامل حل شده است.

#### د) خطای `ارتباط با سرور برقرار نشد. لطفاً اتصال اینترنت را بررسی کنید.`
- **علت:** قطعی اینترنت کلاینت یا خاموش بودن سرویس بک‌اند در Railway.
- **راه‌حل:** سلامت سرویس را از آدرس `/api/health` بررسی کنید. در صورت بازگشت `{"status":"ok","database":"connected"}` ارتباط برقرار است.

