# راهنمای جامع راه‌اندازی نرم‌افزار انبارداری در شبکه محلی ویندوز (Windows Workgroup / LAN)
**واحد اعلام حریق ذوب‌آهن اصفهان**  
**طراح و توسعه‌دهنده:** دکتر احسان ابوالقاسمی

---

## ۱. معماری سیستم: چرا این معماری انتخاب شده است؟
- **SERVER = مرجع حقیقت (Single Source of Truth):**  
  یک کامپیوتر یا سرور ویندوزی در شبکه داخلی به عنوان میزبان اصلی نرم‌افزار و پایگاه داده عمل می‌کند.
- **Client = رابط کاربری (Interface Only):**  
  تمامی کامپیوترها، لپ‌تاپ‌ها، تبلت‌ها و گوشی‌های همراه در انبار صرفاً یک رابط برای ارسال و دریافت اطلاعات هستند و هیچ دیتابیس جداگانه یا اطلاعات آفلاین مستقلی روی کلاینت‌ها ذخیره نمی‌شود.
- **تضمین تراکنش‌های ACID با قفل ردیفی (Row-Level Locking):**  
  تراکنش‌های ورود، خروج و انتقال کالا به صورت اتمیک روی پایگاه داده PostgreSQL سرور انجام می‌شوند تا حتی با ثبت همزمان چند انباردار، موجودی منفی یا تداخل محاسباتی رخ ندهد.
- **بدون وابستگی اینترنتی:**  
  این نرم‌افزار کاملاً به‌صورت آفلاین در شبکه محلی (LAN / Workgroup) کار می‌کند و هیچ نیازی به اتصال به اینترنت ندارد.

---

## ۲. پیش‌نیازهای نصب روی کامپیوتر سرور (Windows)

1. **Node.js (نسخه 20 LTS یا بالاتر):**  
   فایل نصبی Node.js را از سایت رسمی یا به صورت آفلاین دانلود و نصب کنید.
2. **PostgreSQL (نسخه 15 یا 16):**  
   نرم‌افزار PostgreSQL را روی ویندوز نصب کنید.  
   - هنگام نصب، کلمه عبور کاربر `postgres` را تعیین کنید (مثلاً `postgres`).  
   - پورت پیش‌فرض: `5432`.
3. **ایجاد پایگاه داده:**  
   در برنامه **pgAdmin** یا از طریق دستور CMD/PowerShell یک دیتابیس با نام `warehouse_db` بسازید:
   ```sql
   CREATE DATABASE warehouse_db;
   ```

---

## ۳. مراحل نصب و راه‌اندازی روی سرور

### گام اول: انتقال پوشه پروژه
پوشه پروژه `Anbar` را روی سرور در مسیری مناسب (مثلاً `C:\Anbar`) کپی کنید.

### گام دوم: تنظیم متغیرهای محیطی (`.env`)
در ریشه پوشه پروژه، فایلی به نام `.env` بسازید یا ویرایش کنید و اطلاعات اتصال را درج نمایید:
```env
PORT=3000
NODE_ENV=production
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/warehouse_db
JWT_SECRET=zobahan-fire-alarm-wms-lan-secret-key-2026-production
```
*(به جای `YOUR_PASSWORD` کلمه عبور انتخابی خود در هنگام نصب PostgreSQL را وارد کنید)*

### گام سوم: نصب وابستگی‌ها و بیلد اولیه
در CMD یا PowerShell در مسیر پروژه اجرا کنید:
```bash
npm install
npm run build
```
*(اسکریپت `npm run build` تمام بخش فرانت‌اند را به همراه سرور سریع CJS در پوشه `dist` کامپایل می‌کند)*

### گام چهارم: اجرای سرور
برای تست اولیه:
```bash
npm start
```
پیام زیر در ترمینال نمایش داده می‌شود:
```
[Database] Running PostgreSQL auto-migrations...
[Database] PostgreSQL ready with table locking and ACID guarantees.
Server running on http://localhost:3000
```

---

## ۴. تنظیم فایروال ویندوز (Windows Firewall)
برای اینکه سایر کامپیوترها، تبلت‌ها و گوشی‌های موجود در شبکه بتوانند به سرور وصل شوند، باید پورت **3000** در فایروال سرور باز شود.

یکبار پنجره **PowerShell** یا **Command Prompt** را به صورت **Run as Administrator** باز کرده و دستور زیر را کپی و اجرا کنید:
```powershell
netsh advfirewall firewall add rule name="WMS_Warehouse_LAN_3000" dir=in action=allow protocol=TCP localport=3000
```
یا در PowerShell:
```powershell
New-NetFirewallRule -DisplayName "WMS Warehouse Port 3000" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
```

---

## ۵. اجرای خودکار سرور هنگام روشن شدن کامپیوتر (Windows Service / Startup)

برای اینکه با روشن شدن سرور یا ری‌استارت ویندوز، برنامه خودکار بالا بیاید، دو روش ساده وجود دارد:

### روش ۱: استفاده از ابزار PM2 (پیشنهادی و بسیار پایدار)
در ترمینال سرور:
```bash
npm install -g pm2
npm install -g pm2-windows-service
pm2-service-install
pm2 start dist/server.cjs --name "wms-server"
pm2 save
```

### روش ۲: ساخت فایل Bat در Startup ویندوز
یک فایل با نام `start_wms.bat` در پوشه پروژه با محتوای زیر بسازید:
```bat
@echo off
cd /d "C:\Anbar"
npm start
```
سپس کلید `Win + R` را بزنید، عبارت `shell:startup` را تایپ و اینتر کنید، و میانبر (Shortcut) فایل `start_wms.bat` را در آنجا قرار دهید.

---

## ۶. نحوه اتصال سایر سیستم‌ها (کلاینت‌ها) در شبکه محلی

### پیدا کردن آدرس IP سرور:
روی سرور در CMD دستور `ipconfig` را بزنید و آدرس IPv4 سرور را یادداشت کنید (مثلاً `192.168.1.100`).

### اتصال از کامپیوترها و لپ‌تاپ‌ها:
1. کامپیوتر به همان مودم، سوئیچ یا اکسس‌پوینت سرور وصل باشد.
2. مرورگر (Chrome / Edge / Firefox) را باز کنید.
3. در آدرس‌بار وارد کنید:
   ```
   http://192.168.1.100:3000
   ```
4. فرم ورود باز می‌شود. نام کاربری و رمز عبور را وارد کنید:
   - **نام کاربری مدیر پیش‌فرض:** `admin`
   - **رمز عبور:** `admin123`

### اتصال از گوشی و تبلت (با QR Code):
1. از روی سیستم سرور، با حساب `admin` وارد برنامه شوید.
2. از منوی سمت راست گزینه **«اطلاعات سرور و شبکه»** را باز کنید.
3. بارکد QR اختصاصی شبکه محلی نمایش داده می‌شود.
4. با دوربین گوشی یا تبلت متصل به وای‌فای، بارکد را اسکن کنید تا برنامه مستقیماً باز شود.

---

## ۷. نحوه نصب PWA روی کلاینت‌ها بدون نیاز به اینترنت یا گواهی HTTPS
به دلیل استاندارد امنیتی مرورگرها، برای نصب PWA روی بستر شبکه محلی HTTP:
1. در مرورگر Chrome یا Edge کلاینت، آدرس زیر را وارد کنید:
   ```
   chrome://flags/#unsafely-treat-insecure-origin-as-secure
   ```
2. آدرس سرور را وارد کنید: `http://192.168.1.100:3000`
3. وضعیت را روی **Enabled** قرار داده و دکمه **Relaunch** را بزنید.
4. حال دکمه **«نصب نسخه اپلیکیشن (PWA)»** در بالای صفحه فعال شده و نرم‌افزار به صورت یک پنجره مستقل دسکتاپ و بدون نوار آدرس نصب می‌شود.

---

## ۸. رفتار برنامه هنگام قطعی شبکه
- اگر کابل شبکه قطع شود، سوئیچ خاموش شود یا سرور در دسترس نباشد:
  - بلافاصله نشانگر وضعیت به **🔴 شبکه قطع است** تغییر می‌کند.
  - هیچ عملیاتی در کلاینت انجام نمی‌شود و پیام واضح **«شبکه قطع است»** به کاربر نمایش داده می‌شود تا هیچ اطلاعاتی مخدوش یا مغایر نشود.
  - با اتصال مجدد کابل یا روشن شدن سرور، وضعیت به صورت خودکار ظرف چند ثانیه به **🟢 سرور متصل است** بازمی‌گردد.

---

## ۹. پشتیبان‌گیری و بازیابی اطلاعات (Backup & Restore)
- مدیر سیستم می‌تواند از منوی **«پشتیبان‌گیری و بازیابی»** در هر زمان یک نسخه پشتیبان کامل با فرمت JSON دریافت کند.
- این فایل شامل تمام کالاها، انبارها، قفسه‌ها، تراکنش‌ها، کاربران و دفتر ممیزی است و می‌توان آن را روی فلش‌مموری نگه داشت یا در سیستم جدید بازیابی کرد.
