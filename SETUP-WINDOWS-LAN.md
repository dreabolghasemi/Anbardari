# راهنمای جامع راه‌اندازی نرم‌افزار انبارداری در شبکه محلی ویندوز (Windows Workgroup / LAN)
**واحد اعلام حریق ذوب‌آهن اصفهان**  
**طراح و توسعه‌دهنده:** دکتر احسان ابوالقاسمی

---

## ۱. معماری سیستم و اصول پایگاه داده

- **PostgreSQL = تنها و یگانه مرجع حقیقت (Single Source of Truth):**  
  تنها دیتابیس عملیاتی سیستم، پایگاه داده رابطه ای PostgreSQL نصب‌شده روی کامپیوتر سرور مرکزی است. تمامی داده‌ها منحصراً در PostgreSQL ذخیره و مدیریت می‌شوند.
- **حذف کامل ذخیره‌سازی در فایل JSON:**  
  هیچ‌گونه فایل JSON به عنوان دیتابیس عملیاتی (`warehouse-db.json`) در سیستم وجود ندارد. فرمت JSON صرفاً برای پشتیبان‌گیری و خروجی اکسپورت دستی (Export/Backup) توسط مدیر سیستم استفاده می‌شود.
- **معماری Fail-Safe بدون Fallback:**  
  در صورت قطع اتصال یا عدم در دسترس بودن PostgreSQL، سرور هرگز به فایل محلی یا حافظه سوئیچ نمی‌کند؛ بلکه با خطای HTTP 503 و پیام شفاف «دیتابیس سرور در دسترس نیست» مانع از ثبت اطلاعات ناقص یا نامعتبر می‌شود.
- **Client = صرفاً رابط کاربری (Interface Only):**  
  تمام کلاینت‌ها (کامپیوترهای انبار، لپ‌تاپ‌ها، تبلت‌ها و گوشی‌های اندرویدی) به صورت کلاینت سبک (Thin Client) از طریق شبکه محلی به سرور متصل می‌شوند. هیچ کلاینتی دیتابیس مستقل یا آفلاین ندارد.
- **تضمین تراکنش‌های اتمیک (ACID) و قفل ردیفی (Row-Level Locking):**  
  عملیات ورود کالا (`STOCK_IN`)، خروج کالا (`STOCK_OUT`)، انتقال بین انبارها و قفسه‌ها (`TRANSFER`) و اصلاح موجودی کالا به صورت اتمیک درون بلوک‌های تراکنشی `BEGIN`، `COMMIT` و `ROLLBACK` اجرا شده و با دستور `SELECT ... FOR UPDATE` روی ردیف‌های موجودی انبار قفل می‌شوند تا با ثبت همزمان انبارداران، کوچکترین تداخل، خطای محاسباتی یا موجودی منفی رخ ندهد.
- **امنیت بالا و حذف کامل اطلاعات حساس از سورس‌کد:**  
  هیچ کلمه عبور پیش‌فرض یا کلید رمزی در کدهای پروژه هاردکد نشده است. رمز اولیه مدیر از متغیر `ADMIN_INITIAL_PASSWORD` و کلید توکن از `JWT_SECRET` خوانده می‌شود.
- **کاملاً آفلاین در بستر شبکه محلی (LAN):**  
  این نرم‌افزار بدون هیچ وابستگی به اینترنت، کلاود یا سرویس‌های خارجی، به صورت پایدار در شبکه محلی کارخانه ذوب‌آهن اصفهان اجرا می‌شود.

---

## ۲. پیش‌نیازهای نصب روی کامپیوتر سرور (Windows)

1. **Node.js (نسخه 20 LTS یا بالاتر):**  
   فایل نصبی Node.js را از سایت رسمی دانلود و روی سرور ویندوز نصب کنید.
2. **PostgreSQL (نسخه 15 یا 16):**  
   نرم‌افزار PostgreSQL مخصوص ویندوز را نصب کنید.  
   - در مرحله نصب، پسورد کاربر `postgres` را تعیین کنید (مثلاً `MySecurePgPass2026`).  
   - پورت پیش‌فرض: `5432`.
3. **ایجاد پایگاه داده انبارداری:**  
   در برنامه **pgAdmin** یا از طریق ترمینال CMD/PowerShell دستور زیر را اجرا کنید:
   ```sql
   CREATE DATABASE warehouse_db;
   ```
   *(جداول و مایگریشن‌ها با اولین اجرای سرور به صورت خودکار ایجاد می‌شوند).*

---

## ۳. مراحل نصب و راه‌اندازی روی سرور

### گام اول: انتقال پوشه پروژه
پوشه پروژه را روی سرور در مسیری مناسب (مثلاً `C:\Anbardari`) قرار دهید.

### گام دوم: پیکربندی فایل متغیرهای محیطی (`.env`)
در پوشه اصلی پروژه، فایلی به نام `.env` ایجاد کرده یا فایل `.env.example` را کپی نمایید:
```env
PORT=3000
NODE_ENV=production
DATABASE_URL=postgresql://postgres:MySecurePgPass2026@localhost:5432/warehouse_db
JWT_SECRET=zobahan-fire-alarm-wms-lan-secret-key-2026-production
ADMIN_INITIAL_PASSWORD=ZobAhan#2026!Admin
```
*(به جای `MySecurePgPass2026` رمز تعیین‌شده هنگام نصب PostgreSQL و به جای `ZobAhan#2026!Admin` رمز ورود دلخواه حساب مدیر اولیه را وارد کنید).*

### گام سوم: نصب وابستگی‌ها و بیلد نرم‌افزار
یک پنجره Command Prompt یا PowerShell در پوشه پروژه باز کنید:
```bash
npm install
npm run build
```

### گام چهارم: اجرای سرور
برای تست و راه‌اندازی اولیه:
```bash
npm start
```
خروجی در ترمینال:
```
[Database] Running PostgreSQL central migrations...
[Database] Schema migration completed successfully.
[Database] Central PostgreSQL ready with Row-Level Locking and ACID guarantees.
[WMS] Warehouse Management Server running on port 3000
[WMS] App: نرم‌افزار انبارداری واحد اعلام حریق ذوب‌آهن اصفهان
[WMS] Designer: دکتر احسان ابوالقاسمی
```

---

## ۴. تنظیم فایروال ویندوز (Windows Defender Firewall)

برای دسترسی سایر دستگاه‌های شبکه محلی، پورت **3000** را باز کنید:

در پنجره **PowerShell** به صورت **Run as Administrator**:
```powershell
New-NetFirewallRule -DisplayName "WMS Warehouse Port 3000" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
```
یا در **Command Prompt (Admin)**:
```cmd
netsh advfirewall firewall add rule name="WMS_Warehouse_LAN_3000" dir=in action=allow protocol=TCP localport=3000
```

---

## ۵. اجرای خودکار سرور در استارتاپ ویندوز (Windows Service / PM2)

برای پایداری سرور و بالا آمدن خودکار پس از ری‌استارت کامپیوتر:

### روش اول: استفاده از PM2
```bash
npm install -g pm2
npm install -g pm2-windows-service
pm2-service-install
pm2 start dist/server.cjs --name "wms-server"
pm2 save
```

### روش دوم: استفاده از Task Scheduler یا Startup
یک فایل به نام `start_server.bat` در پوشه پروژه ایجاد کنید:
```bat
@echo off
cd /d "C:\Anbardari"
npm start
```
کلید `Win + R` را فشرده، عبارت `shell:startup` را بنویسید و یک Shortcut از فایل `start_server.bat` در آن قرار دهید.

---

## ۶. نحوه اتصال کلاینت‌ها (کامپیوتر، تبلت، موبایل اندروید)

### ۱. پیدا کردن IP سرور:
در سرور دستور `ipconfig` را در ترمینال بزنید (مثلاً `192.168.1.150`).

### ۲. اتصال از طریق مرورگر:
در هر دستگاهی که به شبکه وای‌فای یا کابل متصل است:
```
http://192.168.1.150:3000
```
- **نام کاربری پیش‌فرض:** `admin`
- **کلمه عبور:** مقدار تعیین‌شده در `ADMIN_INITIAL_PASSWORD`

### ۳. اتصال سریع موبایل/تبلت با QR Code:
1. در سیستم سرور با حساب مدیر وارد شوید.
2. از منوی بالا یا سایدبار، بخش **«شبکه و سرور مرکزی»** را باز کنید.
3. کد QR اختصاصی شبکه محلی نمایش داده می‌شود؛ دوربین گوشی را روبه‌روی آن گرفته و بلافاصله به سامانه متصل شوید.

---

## ۷. نصب نرم‌افزار به صورت اپلیکیشن مستقل (PWA) روی کلاینت‌ها

برای نصب آیکون اختصاصی روی دسکتاپ یا صفحه موبایل بدون نوار آدرس مرورگر:
1. در مرورگر Chrome یا Edge کلاینت، آدرس زیر را باز کنید:
   ```
   chrome://flags/#unsafely-treat-insecure-origin-as-secure
   ```
2. آدرس سرور (مثلاً `http://192.168.1.150:3000`) را در کادر وارد کنید.
3. وضعیت را روی **Enabled** قرار داده و مرورگر را **Relaunch** کنید.
4. دکمه **«نصب اپلیکیشن انبارداری»** فعال شده و برنامه به صورت پنجره دسکتاپ مستقل کار خواهد کرد.

---

## ۸. رفتار هوشمند در زمان قطعی کابل یا خاموشی سرور

- کلاینت‌ها به صورت مداوم سلامت اتصال را رصد می‌کنند.
- به محض قطع کابل، سوئیچ یا سرور:
  - نشانگر با رنگ قرمز به **🔴 شبکه قطع است** تبدیل می‌شود.
  - ثبت عملیات غیرفعال شده و پیغام «شبکه قطع است - امکان ثبت فرم وجود ندارد» صادر می‌شود تا اطلاعات متناقض ایجاد نشود.
- با برقراری مجدد ارتباط، سیستم به صورت آنی به وضعیت **🟢 سرور متصل است** بازگشته و آماده فعالیت می‌گردد.

---

## ۹. پشتیبان‌گیری و بازیابی داده‌ها (Backup & Restore)

- مدیر سیستم می‌تواند از بخش **«پشتیبان‌گیری و بازیابی»** در هر زمان یک فایل پشتیبان با فرمت JSON دریافت نماید.
- این فایل صرفاً جهت بایگانی امن روی فلش‌مموری یا انتقال داده‌ها به سرور جدید است و به هیچ عنوان به عنوان دیتابیس زنده استفاده نمی‌شود.
- بازگردانی پشتیبان نیز به صورت یک تراکنش اتمیک روی پایگاه داده مرکزی PostgreSQL سرور اعمال خواهد شد.
