# راهنمای استقرار سامانه انبارداری واحد اعلام حریق ذوب‌آهن اصفهان بر روی Google Cloud

این مستند راهنمای کامل استقرار و راه‌اندازی پایدار سامانه انبارداری بر روی بستر ابری **Google Cloud (سرویس Cloud Run)** می‌باشد.

---

## ۱. استقرار مستقیم با یک کلیک (از داخل AI Studio)

ساده‌ترین و سریع‌ترین راه استقرار:
1. در بالای همین صفحه روی دکمه **Deploy** (یا آیکون Cloud Run) کلیک نمایید.
2. پروژه Google Cloud خود را انتخاب یا تأیید کنید.
3. دکمه تایید استقرار را بزنید؛ سیستم به صورت خودکار برنامه را بیلد کرده و لینک دائمی HTTPS را در اختیارتان می‌گذارد.

---

## ۲. استقرار از طریق دستورات gcloud CLI (کنترل کامل)

چنانچه سورس‌کد را از طریق منوی **Export to ZIP** یا **GitHub** دانلود کرده‌اید:

### گام اول: ورود و تنظیم پروژه
```bash
# ورود به حساب گوگل
gcloud auth login

# انتخاب شناسه پروژه گوگل ابری
gcloud config set project YOUR_PROJECT_ID
```

### گام دوم: فعال‌سازی سرویس‌های لازم در گوگل کلود
```bash
gcloud services enable run.googleapis.com cloudbuild.googleapis.com
```

### گام سوم: استقرار مستقیم برنامه روی Cloud Run
```bash
gcloud run deploy zobahan-fire-warehouse \
  --source . \
  --region europe-west3 \
  --allow-unauthenticated \
  --port 3000 \
  --memory 1Gi \
  --cpu 1 \
  --set-env-vars NODE_ENV=production,JWT_SECRET=zobahan-fire-warehouse-secure-jwt-key-esfahan-steel-co
```

---

## ۳. پایداری داده‌ها (Data Persistence) در Google Cloud Run

سامانه به طور پیش‌فرض اطلاعات انبار و فایل‌ها را در پوشه‌های `/data` و `/uploads` ذخیره می‌کند.
برای اطمینان از ماندگاری دائمی فایل‌ها در استقرارهای چندساله Cloud Run، دو راهکار به شرح زیر در دسترس است:

### راهکار اول (ساده و بی‌هزینه): استفاده از بکاپ دوره‌ای داخل نرم‌افزار
- در هر زمان می‌توانید با ورود به منوی «پشتیبان‌گیری و بازیابی»، با یک کلیک خروجی کامل JSON پایگاه‌داده و اکسل تمام کاردکس‌ها را دریافت نمایید.

### راهکار دوم (سازمانی): اتصال Google Cloud Storage Bucket (GCS Volume Mount)
در سرویس Cloud Run می‌توانید یک باکت ابری (Cloud Storage) را مستقیماً به کانتینر متصل نمایید:
1. یک باکت با نام دلخواه (مثلاً `zobahan-warehouse-data`) در Google Cloud Storage بسازید.
2. در تنظیمات کانتینر Cloud Run در تب **Volumes**:
   - Volume Type: `Cloud Storage Bucket`
   - Mount Path: `/mnt/gcs`
3. متغیرهای محیطی زیر را تنظیم فرمایید:
   - `DATA_DIR_PATH=/mnt/gcs/data`
   - `UPLOADS_DIR_PATH=/mnt/gcs/uploads`
به این ترتیب تمام داده‌ها و فایل‌ها بلافاصله و دائمی در فضای ابری گوگل ذخیره می‌گردند.

---

## ۴. بررسی وضعیت سلامت و پایش (Health Check)

سامانه دارای اندپوینت‌های استاندارد سنجش وضعیت برای پایش ابری گوگل است:
- `/health`
- `/api/health`

خروجی این اندپوینت سلامت دیتابیس، تعداد انبارهای فعال و زمان آماده‌به‌کاری سرور را گزارش می‌دهد.

---
**طراح و برنامه‌نویس:** دکتر احسان ابوالقاسمی  
**واحد متولی:** واحد اعلام حریق شرکت سهامی ذوب‌آهن اصفهان
