# راهنمای جامع استقرار سامانه انبارداری روی GitHub Pages
### سامانه مدیریت انبار واحد اعلام حریق شرکت سهامی ذوب‌آهن اصفهان
**طراح و توسعه‌دهنده:** دکتر احسان ابوالقاسمی

---

## ۱. تفاوت مهم گیت‌هاب پیجز (GitHub Pages) با سرورهای ابری

**گیت‌هاب پیجز (GitHub Pages)** یک بستر میزبانی **استاتیک (Static Web Hosting)** است؛ به این معنی که سرور بک‌اند (Node.js/Express) به صورت سنتی روی آن اجرا نمی‌شود.

برای اینکه بتوانید این سامانه را با **حفظ ۱۰۰٪ قابلیت‌ها و عملکرد کامل** روی GitHub Pages اجرا کنید، نرم‌افزار به یک **معماری هوشمند دوگانه (Dual-Engine Architecture)** مجهز شد:

1. **حالت مستقل مرورگر (Client-Side Storage Engine):**
   - تمامی داده‌ها، کالاها، انبارها، قفسه‌ها، تراکنش‌های ورود و خروج و کاربران در حافظه مرورگر (`localStorage`) ذخیره و به صورت خودکار پایدار می‌شوند.
   - داده‌های اولیه انبار اعلام حریق ذوب‌آهن اصفهان از قبل بارگذاری شده‌اند.
   - گزارش‌های خروجی اکسل (Excel) مستقیماً توسط مرورگر تولید و دانلود می‌شوند.
   - امکان تهیه فایل پشتیبان (Backup) با فرمت JSON و بازیابی مجدد اطلاعات در هر زمان وجود دارد.
2. **حالت هیبریدی (اتصال به سرور ابری):**
   - چنانچه در آینده مایل بودید بک‌اند را روی Google Cloud Run اجرا کنید ولی فرانت‌اند روی GitHub Pages باشد، کافی است آدرس سرور را در بخش «تنظیمات سامانه» وارد کنید تا داده‌ها بین تمام دستگاه‌ها متمرکز شوند.

---

## ۲. مراحل گام‌به‌گام استقرار روی GitHub Pages

### گام اول: ایجاد مخزن در گیت‌هاب (GitHub Repository)
1. وارد حساب کاربری خود در [GitHub](https://github.com) شوید.
2. یک مخزن جدید (Repository) با نام دلخواه (مثلاً `fire-warehouse-wms`) ایجاد کنید.
3. پروژه فعلی را به این مخزن Commit و Push کنید.

---

### گام دوم: فعال‌سازی GitHub Pages با گیت‌هاب اکشنز (GitHub Actions)
گردش کار خودکار (Workflow) در مسیر `.github/workflows/deploy.yml` آماده شده است. برای فعال‌سازی آن:
1. در صفحه مخزن خود در گیت‌هاب، به تب **Settings** بروید.
2. از منوی سمت چپ، بخش **Pages** را انتخاب کنید.
3. در قسمت **Build and deployment**:
   - بخش **Source** را از حالت *Deploy from a branch* به حالت **GitHub Actions** تغییر دهید.
4. تغییرات را ذخیره کنید.

---

### گام سوم: اجرای بیلد و استقرار خودکار
- با هر بار Push کردن تغییرات به شاخه `main` یا `master`، گیت‌هاب به صورت خودکار پروژه را بیلد کرده و روی آدرس اختصاصی شما منتشر می‌کند.
- برای مشاهده وضعیت اجرا، می‌توانید به تب **Actions** در بالای مخزن مراجعه کنید.
- پس از اتمام (حدود ۱ تا ۲ دقیقه)، آدرس وب‌سایت شما به شکل زیر فعال خواهد شد:
  ```text
  https://<نام‌کاربری-شما>.github.io/<نام-مخزن>/
  ```

---

## ۳. مشخصات ورود پیش‌فرض به سامانه

| نقش | نام کاربری | کلمه عبور پیش‌فرض |
| :--- | :--- | :--- |
| **مدیر ارشد (دکتر احسان ابوالقاسمی)** | `admin` | `Ehsan1984*#` |
| **کارشناس انبار** | `operator` | `Operator123*#` |

> 💡 *کلمه عبور در هر زمان از بخش «تنظیمات» قابل تغییر است.*

---

## ۴. حفظ و نگهداری اطلاعات (Backup & Restore)
در حالت استقرار روی GitHub Pages، پیشنهاد می‌شود به صورت دوره‌ای از اطلاعات انبار نسخه پشتیبان تهیه کنید:
1. وارد تب **«پشتیبان‌گیری و رهگیری»** شوید.
2. روی دکمه **«دریافت نسخه پشتیبان (JSON)»** کلیک کنید تا کلیه اقلام، انبارها و لاگ‌های سیستم روی سیستم شما ذخیره شوند.
3. در صورت نیاز به انتقال اطلاعات به دستگاه دیگر، از همان بخش فایل پشتیبان را بارگذاری کنید.
